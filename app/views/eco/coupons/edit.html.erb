<%= simple_form_for [:eco, @coupon] do |f| %>
  <%= render :partial => params[:partial], :locals => {:f => f} %>
  <%= f.submit 'Сохранить', :class => [:btn, 'btn-default'] %>
  <%= link_to 'Отмена', :back %>
<% end %>
