<%= f.input :state, :as => :hidden, :input_html => { :name => :state, :value => :failure_patient } %>
<%= f.input :failure_patient_on, :start_year => Time.zone.now.year, :end_year => Time.zone.now.year - 10 %>
