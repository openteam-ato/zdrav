<%= f.input :partial, :as => :hidden, :input_html => { :name => :partial, :value => :edit_closed } %>

<%= f.input :closed_on, :start_year => Time.zone.now.year, :end_year => Time.zone.now.year - 10 %>
